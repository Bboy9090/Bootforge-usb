[package]
name = "authority-routing"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
device-analysis = { path = "../device-analysis" }
legal-classification = { path = "../legal-classification" }

[dev-dependencies]
rstest = "0.18"
