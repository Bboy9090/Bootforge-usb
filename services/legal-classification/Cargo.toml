[package]
name = "legal-classification"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"
chrono = { version = "=0.4.44", features = ["serde"] }
device-analysis = { path = "../device-analysis" }
ownership-verification = { path = "../ownership-verification" }

[dev-dependencies]
rstest = "=0.18.2"
